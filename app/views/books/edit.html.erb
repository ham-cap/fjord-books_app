<h1><%= Book.human_attribute_name(:editing_book) %></h1>

<%= render 'form', book: @book %>

<%= link_to Book.human_attribute_name(:show), @book %> |
<%= link_to Book.human_attribute_name(:back), books_path %>
